---
import Base from "../layouts/Base.astro";
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";
import { Icon } from "astro-icon/components";

const release = "https://github.com/jincaiw/Paperling/releases/latest";
---

<Base title="Paperling 安装与使用教程" description="Paperling 的 Windows、macOS、Linux 安装、免安装运行、更新与常见问题。">
  <Nav />
  <main id="top" class="mx-auto max-w-4xl px-5 pb-24 pt-32 text-fg">
    <p class="mono-label"><span class="tok-mark">$</span> guide / 中文教程</p>
    <h1 class="font-serif-display mt-3 text-[clamp(2.6rem,7vw,4.5rem)]">安装后，立即开始写作。</h1>
    <p class="mt-5 max-w-2xl text-lg leading-8 text-fg-muted">Paperling 是本地优先的 Markdown 编辑器。下载、安装或直接运行后，按 <kbd>Ctrl</kbd> + <kbd>O</kbd> 打开 Markdown 文件即可开始。</p>

    <section class="mt-14 rounded-2xl border border-line bg-fg/[0.03] p-7" aria-labelledby="download-title">
      <h2 id="download-title" class="text-2xl font-semibold"><Icon name="lucide:download" class="mr-2 inline h-5 w-5" />下载最新版</h2>
      <p class="mt-3 text-fg-muted">所有安装包均在 GitHub Release 发布。请只从官方 Release 页面下载。</p>
      <a href={release} class="btn btn-primary mt-5"><Icon name="lucide:external-link" class="h-4 w-4" />打开 Release 下载页</a>
    </section>

    <section class="mt-16" aria-labelledby="windows-title">
      <h2 id="windows-title" class="font-serif-display text-3xl">Windows 10 / 11</h2>
      <ol class="mt-5 list-decimal space-y-3 pl-6 leading-7 text-fg-muted">
        <li>下载 <code>.msi</code> 或 <code>-setup.exe</code>，双击并按提示完成安装。</li>
        <li>不希望安装时，下载 <code>-portable.exe</code>；它不写入安装目录，双击即可运行。</li>
        <li>首次被 SmartScreen 提示时，核对发布者与下载来源后选择“更多信息 → 仍要运行”。</li>
      </ol>
    </section>

    <section class="mt-14" aria-labelledby="macos-title">
      <h2 id="macos-title" class="font-serif-display text-3xl">macOS</h2>
      <ol class="mt-5 list-decimal space-y-3 pl-6 leading-7 text-fg-muted">
        <li>下载对应架构的 <code>.dmg</code>，打开后将 Paperling 拖入“应用程序”。</li>
        <li>首次打开若被 Gatekeeper 拦截，请在 Finder 中按住 Control 点击应用，选择“打开”。</li>
        <li>以后可从“应用程序”或 Spotlight 启动。</li>
      </ol>
    </section>

    <section class="mt-14" aria-labelledby="linux-title">
      <h2 id="linux-title" class="font-serif-display text-3xl">Linux</h2>
      <div class="mt-5 space-y-5 text-fg-muted">
        <div><p class="font-medium text-fg">Debian / Ubuntu</p><pre><code>sudo apt install ./Paperling_*.deb</code></pre></div>
        <div><p class="font-medium text-fg">Fedora / RHEL</p><pre><code>sudo dnf install ./Paperling-*.rpm</code></pre></div>
        <div><p class="font-medium text-fg">通用 AppImage（免安装）</p><pre><code>chmod +x Paperling_*.AppImage
./Paperling_*.AppImage</code></pre></div>
      </div>
    </section>

    <section class="mt-14" aria-labelledby="first-title">
      <h2 id="first-title" class="font-serif-display text-3xl">第一次使用</h2>
      <ul class="mt-5 list-disc space-y-3 pl-6 leading-7 text-fg-muted">
        <li><strong class="text-fg">打开文件：</strong><kbd>Ctrl</kbd> + <kbd>O</kbd>；使用分屏模式同时编辑和预览。</li>
        <li><strong class="text-fg">命令面板：</strong><kbd>Ctrl</kbd> + <kbd>P</kbd> 搜索功能、文件和标题。</li>
        <li><strong class="text-fg">帮助：</strong>按 <kbd>?</kbd> 查看完整快捷键；设置中的“关于”可查看版本。</li>
        <li><strong class="text-fg">AI：</strong>在“设置 → AI”配置兼容 OpenAI 的服务；密钥存储在操作系统钥匙串。</li>
      </ul>
    </section>

    <section class="mt-14 rounded-2xl border border-line p-7" aria-labelledby="update-title">
      <h2 id="update-title" class="text-2xl font-semibold">更新、卸载与求助</h2>
      <p class="mt-3 text-fg-muted">下载新版安装包覆盖安装即可；Windows Portable 与 AppImage 只需替换旧文件。遇到问题时，请附上系统版本、Paperling 版本和复现步骤，在 GitHub 提交 Issue。</p>
      <a href="https://github.com/jincaiw/Paperling/issues/new/choose" class="btn btn-ghost mt-5"><Icon name="lucide:message-circle" class="h-4 w-4" />提交问题</a>
    </section>
  </main>
  <Footer />
</Base>
